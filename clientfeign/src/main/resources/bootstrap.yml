spring:
  cloud:
    config:
      name: clientfeign #对应的配置文件名称
      uri: http://localhost:8888
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/